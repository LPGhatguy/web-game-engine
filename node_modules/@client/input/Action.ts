enum ActionType {
	Forward,
	Backward,
	Left,
	Right,
	Jump,
	Crouch
};

export { ActionType };

export default class Action {
	type: ActionType;

	constructor(type: ActionType) {
		this.type = type;
	}
}